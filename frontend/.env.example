# Where the backend API runs (see ../backend). Next proxies /api to it. Copy to .env.local to change it.
BACKEND_URL=http://localhost:4000
# Optional: call the API directly from the browser instead of through the proxy.
# Then set the backend's CORS_ORIGINS to this app's origin.
# NEXT_PUBLIC_API_URL=http://localhost:4000
